\name{agri_simulate}
\alias{agri_simulate}
\alias{agri_bootstrap}
\alias{agri_cv}
\alias{agri_power}
\title{Simulation, bootstrap, design-aware cross-validation and power}
\usage{
agri_simulate(object, nsim = 1L, seed = NULL, ...)
agri_bootstrap(object, R = 500L, unit = NULL, type = c("cluster", "parametric"), seed = 123, statistic = c("coef", "prediction"), newdata = NULL, ...)
agri_cv(object, v = 5L, unit = NULL, scheme = c("grouped_kfold", "leave_one_environment_out"), seed = 123, ...)
agri_power(object, term, nsim = 500L, alpha = 0.05, seed = 123, ...)
}
\description{Resampling and simulation utilities that preserve experimental units and avoid random observation-level leakage when clustered validation is required. Cluster bootstrap replicates are relabelled as distinct resampled units. Leave-one-environment-out validation requires environment to be modelled as random because a fixed unseen environment level cannot be extrapolated. Cross-validation reports RMSE and MAE for numeric responses, adds log-loss, Brier score and accuracy for categorical probability predictions, and uses cross-entropy/Brier summaries for compositional or multivariate-count probability matrices. Every function that takes \code{seed} draws under a private seed and restores the caller's random-number state on exit, so a simulation loop that passes a fixed seed does not collapse its replicates onto one realisation.}
\arguments{
  \item{object}{An object of class \code{agri_model}.}
  \item{nsim}{An integer specifying the number of simulations.}
  \item{seed}{An integer specifying the random seed for reproducibility.
The draws are taken under a private seed: the caller's \code{.Random.seed}
is restored when the call returns, so the function never re-seeds the
session. \code{agri_simulate()} accepts \code{NULL} to draw from the
caller's stream instead.}
  \item{R}{An integer specifying the number of bootstrap replicates.}
  \item{unit}{An optional character string specifying the unit for cluster bootstrap.}
  \item{type}{A character string specifying the bootstrap type ("cluster" or "parametric").}
  \item{statistic}{A character string specifying the statistic to bootstrap ("coef" or "prediction").}
  \item{newdata}{An optional data frame for predictions.}
  \item{v}{An integer specifying the number of cross-validation folds.}
  \item{scheme}{A character string specifying the cross-validation scheme.}
  \item{term}{A character string specifying the term for power analysis.}
  \item{alpha}{A numeric value specifying the significance level.}
  \item{...}{Additional arguments passed to methods.}
}
\value{
\code{agri_cv()} returns a list of class \code{agri_cv} with components:
\describe{
  \item{\code{table}}{Per-fold performance table.}
  \item{\code{summary}}{Named numeric summary of the resampling metric.}
  \item{\code{predictions}}{List of held-out predictions.}
  \item{\code{scheme}}{Character string naming the validation scheme.}
  \item{\code{unit}}{Character string naming the resampling unit.}
  \item{\code{seed}}{Seed used for the resampling.}
  \item{\code{model}}{The fitted \code{agri_model} object.}
}

\code{agri_bootstrap()} returns a list of class \code{agri_bootstrap} with
components:
\describe{
  \item{\code{type}}{Character string giving the bootstrap type.}
  \item{\code{R}}{Number of requested bootstrap replicates.}
  \item{\code{successful}}{Number of replicates that succeeded.}
  \item{\code{unit}}{Character string naming the resampled unit.}
  \item{\code{statistic}}{Character string naming the bootstrapped
    statistic.}
  \item{\code{values}}{List of per-replicate results.}
  \item{\code{matrix}}{Matrix of replicate results, one row per
    replicate.}
  \item{\code{seed}}{Seed used for the resampling.}
  \item{\code{model}}{The fitted \code{agri_model} object.}
}

With \code{statistic = "coef"}, the per-replicate fixed effects are read
through a uniform extractor, so mixed backends whose \code{coef()} returns
a list still yield a named vector. If the coefficient matrix cannot be
assembled even so, the function aborts with a message naming the engine and
pointing to \code{statistic = "prediction"}, rather than returning an
all-\code{NA} matrix while \code{successful} reports every replicate as
fine.

\code{agri_simulate()} returns simulated response values generated by the
fitted backend model. \code{agri_power()} returns the result of the
simulation-based power assessment for \code{term}.
}
